name: Mundane Progress

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0,12 * * *'

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up MUNDANE_KEY
        run: |
          echo "${{ secrets.MUNDANE_KEY }}" > ssh_key
          chmod 600 ssh_key
          eval "$(ssh-agent -s)"
          ssh-add ssh_key
          mkdir -p ~/.ssh
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Set up Git
        run: |
          git config --global user.email "danyworks@github.com"
          git config --global user.name "Mundane Works"

      - name: Update Readme
        run: |
          TIMESTAMP=$(date +"%Y%m%d%H%M%S")
          HASH=$(echo -n $TIMESTAMP | sha256sum | cut -c1-7)
          echo "hello, this is today's date: $TIMESTAMP, hash: $HASH" >> MUNDANE_LOG.md

      - name: Commit and push changes
        run: |
          git add MUNDANE_LOG.md
          git commit -m "Update MUNDANE_LOG.md with today's date and hash"
          git push
